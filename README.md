# cell_grid_creator
Cell Grid Creator is a QGIS plugin that automatically generates standardized geographic grid cells within a selected AOI. Users can choose a grid scale, load supported AOI formats, and create grids with cell IDs, coordinates, codes, and names. The output is saved as an ESRI Shapefile in a dedicated CELL_GRID folder.
